# План реализации: [ФУНКЦИЯ]

**Ветка**: `[###-feature-name]` | **Дата**: [ДАТА] | **Спецификация**: [ссылка]
**Ввод**: Спецификация функции из `/specs/[###-feature-name]/spec.md`

**Примечание**: Этот шаблон заполняется командой `/speckit.plan`. См. `.specify/templates/commands/plan.md` для рабочего процесса выполнения.

## Сводка

[Извлечь из спецификации функции: основное требование + технический подход из исследования]

## Технический контекст

<!--
  ТРЕБУЕТСЯ ДЕЙСТВИЕ: Замените содержимое в этом разделе техническими деталями
  для проекта. Структура здесь представлена в консультативном качестве для руководства
  процессом итерации.
-->

**Язык/Версия**: [например, Python 3.11, Swift 5.9, Rust 1.75 или ТРЕБУЕТ УТОЧНЕНИЯ]  
**Основные зависимости**: [например, FastAPI, UIKit, LLVM или ТРЕБУЕТ УТОЧНЕНИЯ]  
**Хранилище**: [если применимо, например, PostgreSQL, CoreData, файлы или N/A]  
**Тестирование**: [например, pytest, XCTest, cargo test или ТРЕБУЕТ УТОЧНЕНИЯ]  
**Целевая платформа**: [например, Linux сервер, iOS 15+, WASM или ТРЕБУЕТ УТОЧНЕНИЯ]
**Тип проекта**: [single/web/mobile - определяет структуру исходников]  
**Цели производительности**: [специфично для домена, например, 1000 req/s, 10k строк/сек, 60 fps или ТРЕБУЕТ УТОЧНЕНИЯ]  
**Ограничения**: [специфично для домена, например, <200ms p95, <100MB памяти, офлайн-способность или ТРЕБУЕТ УТОЧНЕНИЯ]  
**Масштаб/Область**: [специфично для домена, например, 10k пользователей, 1M строк кода, 50 экранов или ТРЕБУЕТ УТОЧНЕНИЯ]

## Проверка конституции

*ШЛЮЗ: Должен пройти перед исследованием Фазы 0. Перепроверить после проектирования Фазы 1.*

[Шлюзы определены на основе файла конституции]

## Структура проекта

### Документация (эта функция)

```text
specs/[###-feature]/
├── plan.md              # Этот файл (вывод команды /speckit.plan)
├── research.md          # Вывод Фазы 0 (команда /speckit.plan)
├── data-model.md        # Вывод Фазы 1 (команда /speckit.plan)
├── quickstart.md        # Вывод Фазы 1 (команда /speckit.plan)
├── contracts/           # Вывод Фазы 1 (команда /speckit.plan)
└── tasks.md             # Вывод Фазы 2 (команда /speckit.tasks - НЕ создается /speckit.plan)
```

### Исходный код (корень репозитория)
<!--
  ТРЕБУЕТСЯ ДЕЙСТВИЕ: Замените дерево плейсхолдеров ниже конкретным макетом
  для этой функции. Удалите неиспользуемые опции и расширьте выбранную структуру
  реальными путями (например, apps/admin, packages/something). Поставленный план не должен
  включать метки Option.
-->

```text
# [УДАЛИТЬ ЕСЛИ НЕ ИСПОЛЬЗУЕТСЯ] Вариант 1: Один проект (ПО УМОЛЧАНИЮ)
src/
├── models/
├── services/
├── cli/
└── lib/

tests/
├── contract/
├── integration/
└── unit/

# [УДАЛИТЬ ЕСЛИ НЕ ИСПОЛЬЗУЕТСЯ] Вариант 2: Веб-приложение (когда обнаружены "frontend" + "backend")
backend/
├── src/
│   ├── models/
│   ├── services/
│   └── api/
└── tests/

frontend/
├── src/
│   ├── components/
│   ├── pages/
│   └── services/
└── tests/

# [УДАЛИТЬ ЕСЛИ НЕ ИСПОЛЬЗУЕТСЯ] Вариант 3: Мобильное + API (когда обнаружены "iOS/Android")
api/
└── [то же, что backend выше]

ios/ или android/
└── [специфичная для платформы структура: модули функций, UI потоки, платформенные тесты]
```

**Решение о структуре**: [Документировать выбранную структуру и ссылаться на реальные
директории, захваченные выше]

## Отслеживание сложности

> **Заполняйте ТОЛЬКО если Проверка конституции имеет нарушения, которые должны быть обоснованы**

| Нарушение | Почему нужно | Более простое альтернативное решение отклонено, потому что |
|-----------|--------------|-----------------------------------------------------------|
| [например, 4-й проект] | [текущая потребность] | [почему 3 проекта недостаточно] |
| [например, Паттерн Repository] | [конкретная проблема] | [почему прямой доступ к БД недостаточен] |
