---
description: Создать пользовательский чеклист для текущей функции на основе требований пользователя.
---

## Назначение чеклиста: "Модульные тесты для требований"

**КРИТИЧЕСКОЕ ПОНЯТИЕ**: Чеклисты — это **МОДУЛЬНЫЕ ТЕСТЫ ДЛЯ НАПИСАНИЯ ТРЕБОВАНИЙ** — они проверяют качество, ясность и полноту требований в заданной области.

**НЕ для проверки/тестирования**:

- ❌ НЕ "Проверить, что кнопка кликается правильно"
- ❌ НЕ "Протестировать обработку ошибок"
- ❌ НЕ "Подтвердить, что API возвращает 200"
- ❌ НЕ проверка соответствия кода/реализации спецификации

**ДЛЯ проверки качества требований**:

- ✅ "Определены ли требования к визуальной иерархии для всех типов карточек?" (полнота)
- ✅ "Квантифицировано ли 'выделенное отображение' с конкретными размерами/позиционированием?" (ясность)
- ✅ "Согласованы ли требования к состояниям наведения для всех интерактивных элементов?" (согласованность)
- ✅ "Определены ли требования доступности для навигации с клавиатуры?" (покрытие)
- ✅ "Определяет ли спецификация, что происходит при ошибке загрузки изображения логотипа?" (граничные случаи)

**Метафора**: Если ваша спецификация — это код, то чеклист — это его набор модульных тестов. Вы проверяете, хорошо ли написаны требования, полны ли они, однозначны ли и готовы ли к реализации — НЕ работает ли реализация.

## Ввод пользователя

```text
$ARGUMENTS
```

Вы **ДОЛЖНЫ** учитывать ввод пользователя перед продолжением (если он не пустой).

## Шаги выполнения

1. **Настройка**: Запустите `.specify/scripts/powershell/check-prerequisites.ps1 -Json` из корня репозитория и распарсите JSON для FEATURE_DIR и списка AVAILABLE_DOCS.
   - Все пути к файлам должны быть абсолютными.
   - Для одинарных кавычек в аргументах типа "I'm Groot" используйте синтаксис экранирования: например, 'I'\''m Groot' (или двойные кавычки, если возможно: "I'm Groot").

2. **Уточнение намерения (динамическое)**: Выведите до ТРЕХ начальных контекстных уточняющих вопросов (без предварительно подготовленного каталога). Они ДОЛЖНЫ:
   - Генерироваться из формулировки пользователя + извлеченных сигналов из spec/plan/tasks
   - Задаваться только об информации, которая существенно меняет содержание чеклиста
   - Пропускаться индивидуально, если уже однозначны в `$ARGUMENTS`
   - Предпочитать точность широте

   Алгоритм генерации:
   1. Извлечь сигналы: ключевые слова домена функции (например, auth, latency, UX, API), индикаторы риска ("critical", "must", "compliance"), подсказки заинтересованных сторон ("QA", "review", "security team") и явные результаты ("a11y", "rollback", "contracts").
   2. Сгруппировать сигналы в области фокуса-кандидаты (макс. 4), ранжированные по релевантности.
   3. Определить вероятную аудиторию и время (автор, рецензент, QA, релиз), если не указано явно.
   4. Обнаружить недостающие измерения: широта области, глубина/строгость, акцент на рисках, границы исключений, измеримые критерии приемки.
   5. Сформулировать вопросы, выбранные из этих архетипов:
      - Уточнение области (например, "Должно ли это включать точки интеграции с X и Y или оставаться ограниченным локальной корректностью модуля?")
      - Приоритизация рисков (например, "Какие из этих потенциальных областей риска должны получить обязательные проверки?")
      - Калибровка глубины (например, "Это легкий список проверки перед коммитом или формальный шлюз релиза?")
      - Обрамление аудитории (например, "Будет ли это использоваться только автором или коллегами во время PR-ревью?")
      - Границы исключений (например, "Должны ли мы явно исключить элементы настройки производительности в этом раунде?")
      - Пробел в классах сценариев (например, "Обнаружены потоки восстановления — входят ли пути отката / частичного отказа в область?")

   Правила форматирования вопросов:
   - При представлении опций создайте компактную таблицу с колонками: Опция | Кандидат | Почему это важно
   - Ограничьтесь максимум опциями A–E; опустите таблицу, если свободный ответ понятнее
   - Никогда не просите пользователя повторить то, что он уже сказал
   - Избегайте спекулятивных категорий (без галлюцинаций). Если не уверены, спросите явно: "Подтвердите, принадлежит ли X области."

   Значения по умолчанию при невозможности взаимодействия:
   - Глубина: Стандартная
   - Аудитория: Рецензент (PR), если связано с кодом; Автор в противном случае
   - Фокус: Топ-2 кластера релевантности

   Выведите вопросы (помеченные Q1/Q2/Q3). После ответов: если ≥2 классов сценариев (Альтернативный / Исключение / Восстановление / Нефункциональный домен) остаются неясными, вы МОЖЕТЕ задать до ДВУХ дополнительных целевых вопросов (Q4/Q5) с однострочным обоснованием каждого (например, "Неразрешенный риск пути восстановления"). Не превышайте пять вопросов всего. Пропустите эскалацию, если пользователь явно отказывается от дополнительных.

3. **Понять запрос пользователя**: Объедините `$ARGUMENTS` + уточняющие ответы:
   - Выведите тему чеклиста (например, security, review, deploy, ux)
   - Объедините явные обязательные элементы, упомянутые пользователем
   - Сопоставьте выбор фокуса с каркасом категорий
   - Выведите недостающий контекст из spec/plan/tasks (НЕ галлюцинируйте)

4. **Загрузить контекст функции**: Прочитайте из FEATURE_DIR:
   - spec.md: Требования и область функции
   - plan.md (если существует): Технические детали, зависимости
   - tasks.md (если существует): Задачи реализации

   **Стратегия загрузки контекста**:
   - Загружайте только необходимые части, релевантные активным областям фокуса (избегайте полного дампа файлов)
   - Предпочитайте суммирование длинных разделов в краткие маркеры сценариев/требований
   - Используйте прогрессивное раскрытие: добавляйте последующее извлечение только при обнаружении пробелов
   - Если исходные документы большие, генерируйте промежуточные элементы сводки вместо встраивания сырого текста

5. **Создать чеклист** — Создать "Модульные тесты для требований":
   - Создайте директорию `FEATURE_DIR/checklists/`, если она не существует
   - Сгенерируйте уникальное имя файла чеклиста:
     - Используйте короткое, описательное имя на основе домена (например, `ux.md`, `api.md`, `security.md`)
     - Формат: `[domain].md`
     - Если файл существует, добавьте к существующему файлу
   - Нумеруйте элементы последовательно, начиная с CHK001
   - Каждый запуск `/speckit.checklist` создает НОВЫЙ файл (никогда не перезаписывает существующие чеклисты)

   **ОСНОВНОЙ ПРИНЦИП — Тестировать требования, а не реализацию**:
   Каждый элемент чеклиста ДОЛЖЕН оценивать САМИ ТРЕБОВАНИЯ на:
   - **Полноту**: Присутствуют ли все необходимые требования?
   - **Ясность**: Однозначны ли и конкретны ли требования?
   - **Согласованность**: Согласуются ли требования друг с другом?
   - **Измеримость**: Можно ли объективно проверить требования?
   - **Покрытие**: Охвачены ли все сценарии/граничные случаи?

   **Структура категорий** — Группировать элементы по измерениям качества требований:
   - **Полнота требований** (Документированы ли все необходимые требования?)
   - **Ясность требований** (Конкретны ли и однозначны ли требования?)
   - **Согласованность требований** (Согласуются ли требования без конфликтов?)
   - **Качество критериев приемки** (Измеримы ли критерии успеха?)
   - **Покрытие сценариев** (Охвачены ли все потоки/случаи?)
   - **Покрытие граничных случаев** (Определены ли граничные условия?)
   - **Нефункциональные требования** (Производительность, Безопасность, Доступность и т.д. — указаны ли они?)
   - **Зависимости и предположения** (Документированы ли и проверены ли они?)
   - **Неоднозначности и конфликты** (Что требует уточнения?)

   **КАК ПИСАТЬ ЭЛЕМЕНТЫ ЧЕКЛИСТА — "Модульные тесты для требований"**:

   ❌ **НЕПРАВИЛЬНО** (Тестирование реализации):
   - "Проверить, что главная страница отображает 3 карточки эпизодов"
   - "Протестировать, что состояния наведения работают на десктопе"
   - "Подтвердить, что клик по логотипу ведет на главную"

   ✅ **ПРАВИЛЬНО** (Тестирование качества требований):
   - "Указано ли точное количество и расположение рекомендуемых эпизодов?" [Полнота]
   - "Квантифицировано ли 'выделенное отображение' с конкретными размерами/позиционированием?" [Ясность]
   - "Согласованы ли требования к состояниям наведения для всех интерактивных элементов?" [Согласованность]
   - "Определены ли требования навигации с клавиатуры для всего интерактивного UI?" [Покрытие]
   - "Указано ли поведение при отказе при загрузке изображения логотипа?" [Граничные случаи]
   - "Определены ли состояния загрузки для асинхронных данных эпизодов?" [Полнота]
   - "Определяет ли спецификация визуальную иерархию для конкурирующих элементов UI?" [Ясность]

   **СТРУКТУРА ЭЛЕМЕНТА**:
   Каждый элемент должен следовать этому шаблону:
   - Формат вопроса о качестве требования
   - Фокус на том, что НАПИСАНО (или не написано) в spec/plan
   - Включите измерение качества в квадратных скобках [Полнота/Ясность/Согласованность/и т.д.]
   - Ссылайтесь на раздел спецификации `[Spec §X.Y]` при проверке существующих требований
   - Используйте маркер `[Пробел]` при проверке отсутствующих требований

   **ПРИМЕРЫ ПО ИЗМЕРЕНИЯМ КАЧЕСТВА**:

   Полнота:
   - "Определены ли требования обработки ошибок для всех режимов отказа API? [Пробел]"
   - "Указаны ли требования доступности для всех интерактивных элементов? [Полнота]"
   - "Определены ли требования точек останова для мобильных устройств для адаптивных макетов? [Пробел]"

   Ясность:
   - "Квантифицирована ли 'быстрая загрузка' с конкретными порогами времени? [Ясность, Spec §NFR-2]"
   - "Явно ли определены критерии выбора 'связанных эпизодов'? [Ясность, Spec §FR-5]"
   - "Определено ли 'выделенное' с измеримыми визуальными свойствами? [Неоднозначность, Spec §FR-4]"

   Согласованность:
   - "Согласуются ли требования навигации на всех страницах? [Согласованность, Spec §FR-10]"
   - "Согласованы ли требования компонентов карточек между главной и страницей деталей? [Согласованность]"

   Покрытие:
   - "Определены ли требования для сценариев нулевого состояния (нет эпизодов)? [Покрытие, Граничный случай]"
   - "Охвачены ли сценарии одновременного взаимодействия пользователей? [Покрытие, Gap]"
   - "Указаны ли требования для частичных отказов загрузки данных? [Покрытие, Поток исключений]"

   Измеримость:
   - "Измеримы/проверяемы ли требования визуальной иерархии? [Критерии приемки, Spec §FR-1]"
   - "Можно ли объективно проверить 'сбалансированный визуальный вес'? [Измеримость, Spec §FR-2]"

   **Классификация и покрытие сценариев** (Фокус на качестве требований):
   - Проверьте, существуют ли требования для: Основных, Альтернативных, Исключительных/Ошибочных, Восстановительных, Нефункциональных сценариев
   - Для каждого класса сценариев спросите: "Полны ли, ясны ли и согласованы ли требования [типа сценария]?"
   - Если класс сценария отсутствует: "Намеренно ли исключены или отсутствуют требования [типа сценария]? [Пробел]"
   - Включите устойчивость/откат при изменении состояния: "Определены ли требования отката для отказов миграции? [Пробел]"

   **Требования прослеживаемости**:
   - МИНИМУМ: ≥80% элементов ДОЛЖНЫ включать хотя бы одну ссылку прослеживаемости
   - Каждый элемент должен ссылаться на: раздел спецификации `[Spec §X.Y]` или использовать маркеры: `[Пробел]`, `[Неоднозначность]`, `[Конфликт]`, `[Предположение]`
   - Если системы ID не существует: "Установлена ли схема ID требований и критериев приемки? [Прослеживаемость]"

   **Выявление и разрешение проблем** (Проблемы качества требований):
   Задавайте вопросы о самих требованиях:
   - Неоднозначности: "Квантифицирован ли термин 'быстрый' с конкретными метриками? [Неоднозначность, Spec §NFR-1]"
   - Конфликты: "Конфликтуют ли требования навигации между §FR-10 и §FR-10a? [Конфликт]"
   - Предположения: "Проверено ли предположение о 'всегда доступном API подкастов'? [Предположение]"
   - Зависимости: "Документированы ли требования внешнего API подкастов? [Зависимость, Gap]"
   - Отсутствующие определения: "Определена ли 'визуальная иерархия' с измеримыми критериями? [Пробел]"

   **Консолидация содержимого**:
   - Мягкий лимит: Если сырых элементов-кандидатов > 40, приоритизируйте по риску/влиянию
   - Объединяйте почти дубликаты, проверяющие тот же аспект требования
   - Если >5 граничных случаев с низким влиянием, создайте один элемент: "Охвачены ли граничные случаи X, Y, Z в требованиях? [Покрытие]"

   **🚫 СТРОГО ЗАПРЕЩЕНО** — Это делает тест реализации, а не тест требований:
   - ❌ Любой элемент, начинающийся с "Проверить", "Протестировать", "Подтвердить", "Проверить" + поведение реализации
   - ❌ Ссылки на выполнение кода, действия пользователя, поведение системы
   - ❌ "Отображается правильно", "работает должным образом", "функционирует как ожидается"
   - ❌ "Клик", "навигация", "рендеринг", "загрузка", "выполнение"
   - ❌ Тестовые случаи, планы тестирования, процедуры QA
   - ❌ Детали реализации (фреймворки, API, алгоритмы)

   **✅ ОБЯЗАТЕЛЬНЫЕ ШАБЛОНЫ** — Они проверяют качество требований:
   - ✅ "Определены/указаны/документированы ли [тип требования] для [сценария]?"
   - ✅ "Квантифицирован/уточнен ли [нечеткий термин] с конкретными критериями?"
   - ✅ "Согласованы ли требования между [раздел A] и [раздел B]?"
   - ✅ "Можно ли объективно измерить/проверить [требование]?"
   - ✅ "Охвачены ли [граничные случаи/сценарии] в требованиях?"
   - ✅ "Определяет ли спецификация [отсутствующий аспект]?"

6. **Ссылка на структуру**: Сгенерируйте чеклист, следуя каноническому шаблону в `.specify/templates/checklist-template.md` для заголовка, мета-раздела, заголовков категорий и форматирования ID. Если шаблон недоступен, используйте: заголовок H1, мета-строки назначения/создания, разделы `##` категорий, содержащие строки `- [ ] CHK### <элемент требования>` с глобально инкрементирующимися ID, начиная с CHK001.

7. **Отчет**: Выведите полный путь к созданному чеклисту, количество элементов и напомните пользователю, что каждый запуск создает новый файл. Обобщите:
   - Выбранные области фокуса
   - Уровень глубины
   - Актор/время
   - Любые явные обязательные элементы, указанные пользователем

**Важно**: Каждый вызов команды `/speckit.checklist` создает файл чеклиста, используя короткие, описательные имена, если файл уже не существует. Это позволяет:

- Множественные чеклисты разных типов (например, `ux.md`, `test.md`, `security.md`)
- Простые, запоминающиеся имена файлов, указывающие назначение чеклиста
- Легкую идентификацию и навигацию в папке `checklists/`

Чтобы избежать беспорядка, используйте описательные типы и очищайте устаревшие чеклисты по завершении.

## Типы примеров чеклистов и примеры элементов

**Качество требований UX:** `ux.md`

Примеры элементов (тестирование требований, НЕ реализации):

- "Определены ли требования визуальной иерархии с измеримыми критериями? [Ясность, Spec §FR-1]"
- "Явно ли указаны количество и позиционирование элементов UI? [Полнота, Spec §FR-1]"
- "Согласованы ли требования состояний взаимодействия (hover, focus, active)? [Согласованность]"
- "Указаны ли требования доступности для всех интерактивных элементов? [Покрытие, Gap]"
- "Определено ли поведение при отказе при загрузке изображений? [Граничный случай, Gap]"
- "Можно ли объективно измерить 'выделенное отображение'? [Измеримость, Spec §FR-4]"

**Качество требований API:** `api.md`

Примеры элементов:

- "Указаны ли форматы ответов об ошибках для всех сценариев отказа? [Полнота]"
- "Квантифицированы ли требования ограничения скорости с конкретными порогами? [Ясность]"
- "Согласованы ли требования аутентификации для всех конечных точек? [Согласованность]"
- "Определены ли требования повторных попыток/таймаутов для внешних зависимостей? [Покрытие, Gap]"
- "Документирована ли стратегия версионирования в требованиях? [Пробел]"

**Качество требований производительности:** `performance.md`

Примеры элементов:

- "Квантифицированы ли требования производительности с конкретными метриками? [Ясность]"
- "Определены ли цели производительности для всех критических пользовательских путей? [Покрытие]"
- "Указаны ли требования производительности при различных условиях нагрузки? [Полнота]"
- "Можно ли объективно измерить требования производительности? [Измеримость]"
- "Определены ли требования деградации для сценариев высокой нагрузки? [Граничный случай, Gap]"

**Качество требований безопасности:** `security.md`

Примеры элементов:

- "Указаны ли требования аутентификации для всех защищенных ресурсов? [Покрытие]"
- "Определены ли требования защиты данных для конфиденциальной информации? [Полнота]"
- "Документирована ли модель угроз и согласованы ли требования с ней? [Прослеживаемость]"
- "Согласуются ли требования безопасности с обязательствами по соответствию? [Согласованность]"
- "Определены ли требования реагирования на отказы/нарушения безопасности? [Gap, Поток исключений]"

## Антипримеры: Что НЕ делать

**❌ НЕПРАВИЛЬНО — Это тестирует реализацию, а не требования:**

```markdown
- [ ] CHK001 - Проверить, что главная страница отображает 3 карточки эпизодов [Spec §FR-001]
- [ ] CHK002 - Протестировать, что состояния наведения работают правильно на десктопе [Spec §FR-003]
- [ ] CHK003 - Подтвердить, что клик по логотипу ведет на главную страницу [Spec §FR-010]
- [ ] CHK004 - Проверить, что раздел связанных эпизодов показывает 3-5 элементов [Spec §FR-005]
```

**✅ ПРАВИЛЬНО — Это проверяет качество требований:**

```markdown
- [ ] CHK001 - Явно ли указаны количество и макет рекомендуемых эпизодов? [Полнота, Spec §FR-001]
- [ ] CHK002 - Согласованы ли требования состояний наведения для всех интерактивных элементов? [Согласованность, Spec §FR-003]
- [ ] CHK003 - Ясны ли требования навигации для всех кликабельных элементов бренда? [Ясность, Spec §FR-010]
- [ ] CHK004 - Документированы ли критерии выбора связанных эпизодов? [Gap, Spec §FR-005]
- [ ] CHK005 - Определены ли требования состояний загрузки для асинхронных данных эпизодов? [Пробел]
- [ ] CHK006 - Можно ли объективно измерить требования 'визуальной иерархии'? [Измеримость, Spec §FR-001]
```

**Ключевые различия:**

- Неправильно: Проверяет, работает ли система правильно
- Правильно: Проверяет, правильно ли написаны требования
- Неправильно: Проверка поведения
- Правильно: Валидация качества требований
- Неправильно: "Делает ли это X?"
- Правильно: "Явно ли указано X?"
